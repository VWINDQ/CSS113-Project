# 🕸️ Graph Algorithms Visualizer

โปรเจกต์นี้เป็น Interactive Web Application ที่สร้างด้วย **Streamlit** และ **NetworkX** เพื่อช่วยให้เข้าใจการทำงานของอัลกอริทึมกราฟ (Graph Algorithms) แบบทีละขั้นตอน (Step-by-Step)

## 🧠 Supported Algorithms

### 1. Breadth-First Search (BFS) - การค้นหาแบบแนวกว้าง
**"ค่อยๆ กวาดไปรอบตัว ทีละชั้น"**
* **หลักการ:** เริ่มจากจุดเริ่มต้น แล้วสำรวจเพื่อนบ้านรอบๆ ตัวให้ครบทุกทิศทางก่อน (ชั้นที่ 1) จากนั้นค่อยขยับไปสำรวจเพื่อนของเพื่อน (ชั้นที่ 2) ต่อไปเรื่อยๆ
* **เปรียบเทียบ:** เหมือนการโยนหินลงน้ำ แล้วคลื่นค่อยๆ กระจายออกเป็นวงกลม
* **การใช้งาน:** เหมาะสำหรับหาเส้นทางที่สั้นที่สุดในกราฟที่ *ไม่มีน้ำหนัก* (Unweighted Graph)

### 2. Depth-First Search (DFS) - การค้นหาแบบแนวลึก
**"เดินหน้าไปให้สุดซอย แล้วค่อยถอยกลับ"**
* **หลักการ:** เลือกเส้นทางหนึ่งแล้วเดินลึกเข้าไปเรื่อยๆ จนกว่าจะไปต่อไม่ได้ (ชนกำแพง) แล้วค่อยย้อนกลับมา (Backtrack) เพื่อหาทางแยกอื่นที่ยังไม่ได้ไป
* **เปรียบเทียบ:** เหมือนการเดินเขาวงกต ที่เราจะเดินเกาะกำแพงไปเรื่อยๆ จนเจอทางตันแล้วค่อยถอยกลับมาทางแยกเดิม
* **การใช้งาน:** ใช้ตรวจสอบว่ากราฟเชื่อมต่อกันหรือไม่ หรือใช้แก้ปัญหาเขาวงกต

### 3. Dijkstra's Algorithm - อัลกอริทึมไดก์สตรา
**"ระบบ GPS หาเส้นทางที่สั้นที่สุด"**
* **หลักการ:** เป็นการค้นหาเส้นทางจากจุดเริ่มต้นไปยังจุดหมาย โดยคำนึงถึง "ต้นทุน" หรือ "ระยะทาง" (Weight) ของแต่ละเส้นทาง อัลกอริทึมจะเลือกเดินไปหาจุดที่ใกล้ที่สุดเสมอ และคอยอัปเดตระยะทางถ้ารู้ว่ามีทางอื่นที่สั้นกว่า
* **เปรียบเทียบ:** เหมือน Google Maps ที่คำนวณระยะทางและรถติด เพื่อหาทางที่ใช้เวลาน้อยที่สุด
* **การใช้งาน:** หา Shortest Path ในกราฟที่มีน้ำหนัก (Weighted Graph) ที่เป็นบวก

### 4. Minimum Spanning Tree (MST)
คือการหาเส้นเชื่อม (Edge) เพื่อให้ทุกจุด (Node) เชื่อมต่อกันทั้งหมด โดยใช้ผลรวมน้ำหนักน้อยที่สุด (ประหยัดที่สุด) และไม่เกิดวงวน (Loop)

#### 🅰️ Kruskal's Algorithm
**"เลือกเส้นที่ถูกที่สุดก่อน เสมอ"**
* **หลักการ:** เรียงลำดับเส้นเชื่อมทั้งหมดจาก *ถูกที่สุด* ไป *แพงที่สุด* แล้วค่อยๆ หยิบเส้นมาเชื่อมจุดเข้าด้วยกัน ถ้าหยิบมาแล้วทำให้เกิดวงกลม (Cycle) ให้ข้ามเส้นนั้นไป ทำจนกว่าจะครบทุกจุด
* **จุดเด่น:** มองภาพรวมของเส้นเชื่อมทั้งกราฟ

#### 🅱️ Prim's Algorithm
**"ค่อยๆ งอกอาณาเขต จากจุดเริ่มต้น"**
* **หลักการ:** เริ่มจากจุดหนึ่งจุด แล้วมองหาเส้นเชื่อมที่ออกจากจุดที่เรายืนอยู่ไปยังจุดใหม่ที่ *ใกล้ที่สุด* (ถูกที่สุด) แล้วขยับขยายอาณาเขตไปเรื่อยๆ จนครอบคลุมทุกจุด
* **จุดเด่น:** ค่อยๆ ขยายกิ่งก้านสาขาออกจากกลุ่มก้อนเดิม (Cluster)

---
*Built with ❤️ using Python, Streamlit, and NetworkX*
